function discribe_city (city: string, country: string = 'Pakistan') {
  console.log(`${city} is in ${country}`);
}

// calling the function
discribe_city ('Karachi');
discribe_city ('Lahore');
discribe_city ('Berlin', 'Germani');